Vendored from:

- Repository: `https://github.com/Physical-Intelligence/openpi.git`
- Source subdirectory: `packages/openpi-client`
- Source commit: `981483dca0fd9acba698fea00aa6e52d56a66c58`

This copy is tracked directly in `serl_torch` as a vendored third-party package.

Upstream license:

- Apache License 2.0, copied into `third_party/openpi-client/LICENSE`

Installation:

```bash
pip install -e ./third_party/openpi-client
```

This package only provides the Python client import path:

- `openpi_client.*`

It does not replace a full OpenPI checkout for serving the OpenPI policy runtime.
