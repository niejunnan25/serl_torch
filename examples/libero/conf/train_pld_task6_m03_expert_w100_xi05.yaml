defaults:
  - train_pld_task6
  - _self_

residual:
  xi: 0.5

openpi:
  port: 32343

env:
  remote:
    port: 31343

offline:
  dataset_paths:
    - data/offline/libero_10_task_6
  bootstrap_base:
    enabled: false

training:
  warmup_base_episodes: 100
  async_eval:
    enabled: true
    env_port: 31443
  checkpoint_dir: artifacts/checkpoints

logging:
  step_log_file: logs/step_logs.jsonl
  episode_log_file: logs/episode_logs.jsonl
  summary_file: logs/summary.json

hydra:
  run:
    dir: outputs/libero/pld_matrix/task6/m03_expert_w100_xi05/${now:%Y-%m-%d}/${now:%H-%M-%S}
