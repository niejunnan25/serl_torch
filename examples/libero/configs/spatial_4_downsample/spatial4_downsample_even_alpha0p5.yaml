# @package _global_
defaults:
  - /spatial_4_0514_runtime/spatial4_chunk_alpha0p5_unfiltered_offline_noent_std5p0_ports53900@_global_
  - _self_

runtime:
  trainer_port: 62204
  broadcast_port: 62205
  trainer_transport:
    data_port: 62206

processor_transport:
  port: 62207

wandb:
  exp_name: spatial4_downsample_even_alpha0p5
  group: libero_spatial_task4_downsample

policy:
  port: 62201

backfill_policy:
  port: 62202

env:
  remote:
    port: 62200

action_downsample:
  enabled: true
  factor: 2
  offset: 0

replay:
  prepared_chunk:
    offline_enabled: false

offline:
  enabled: false
  prepared_path: null
  ratio: 0.0
  pretrain_steps: 0

training:
  async_eval:
    env:
      remote:
        port: 62203

launch:
  output_root: outputs/spatial_4_downsample/spatial4_downsample_even_alpha0p5

notes:
  experiment_note: action downsample even; base policy selects a0,a2,a4,a6,a8 and trains online only
  changed_fields:
  - action_downsample.enabled=true
  - action_downsample.factor=2
  - action_downsample.offset=0
  - offline.enabled=false
  - replay.prepared_chunk.offline_enabled=false
